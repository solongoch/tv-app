import { AbstractExtendedWebDriver } from 'protractor/built/browser';
import { Url } from 'url';

export interface IShowinfo {
  name: string
  image: string
  description: string
  country: string
  genre: string
  runtime: number
}
